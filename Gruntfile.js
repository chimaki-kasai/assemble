module.exports = function(grunt) {

  grunt.task.loadNpmTasks('assemble');

/* ==========================================================================
   タスク一覧
   ========================================================================== */
  grunt.initConfig({
    
  }); // grunt.initConfig

/* ==========================================================================
   タスク定義
   ========================================================================== */
  // ↓（'ターミナルで実行するコマンド名', ['実行したいgruntのタスク名']）
  grunt.registerTask('', ['']);

};
